/**
 * Resource Service connection configuration
 * Shared configuration for connecting to OpenBlock Resource Service
 */

const RESOURCE_SERVICE_HOST = 'localhost';
const RESOURCE_SERVICE_PORT = 20111;

module.exports = {
    RESOURCE_SERVICE_HOST,
    RESOURCE_SERVICE_PORT
};
