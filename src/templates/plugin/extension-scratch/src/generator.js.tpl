// Generator for upload mode blocks
// This file is only needed if your extension supports upload mode (realtime + upload)

export default Blockly => {

    // Example upload mode block generator (uncomment and modify as needed):
    // Blockly.Arduino.{{pluginId}}_uploadModeBlock = function (block) {
    //     const value = Blockly.Arduino.valueToCode(block, 'VALUE', Blockly.Arduino.ORDER_ATOMIC) || '100';
    //
    //     Blockly.Arduino.includes_.{{pluginId}} = `// Include your library here`;
    //     Blockly.Arduino.definitions_.{{pluginId}} = `// Define variables here`;
    //
    //     return `// Generated code for value ${value}\n`;
    // };

    return Blockly;
};

